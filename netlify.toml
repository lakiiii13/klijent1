[build]
  command = "npm run build"
  publish = "dist"

# SPA — sve rute na index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Ako API hostuješ na Renderu, odkomentariši i zameni URL:
# [[redirects]]
#   from = "/api/*"
#   to = "https://TVOJ-SERVIS.onrender.com/api/:splat"
#   status = 200
#   force = true
